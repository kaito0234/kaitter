<% provide(:title, 'All users') %>
<h1>All users</h1>

  <% if current_user.admin? && !current_user?(@user) %>
    <%= link_to "全ユーザーのlogin conditionを更新", user_logincondition_false_users_path,data: { confirm: "本当に更新しますか？" } %>
  <% end %>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
