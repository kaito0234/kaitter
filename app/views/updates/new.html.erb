<% provide(:title, 'create update') %>
<% provide(:button_text, 'アップデート情報を作成する') %>
<h1>アップデート情報作成</h1>
<div>
  <% if logged_in? %>
    <% if current_user.admin? && !current_user?(@user) %>
      <%= render 'form' %>
    <% end %>
  <% end %>
</div>
