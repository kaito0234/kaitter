<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      <%# </section>
      <section class="stats"> %>
      <div class="user_margin">
        <%= render 'shared/stats' %>
      </div>
      </section>
      <%# <section calass="micropost_form">
        <%= render 'shared/micropost_form' %>
      <%# </section>  %>
    </aside>
    <div class="col-md-8">
      <h3>ツイート一覧</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>Welcome to the "Kaitter Japan" </h1>
    <h2>
      This is the home page of the
      <a href="#">Kaitter Japan</a>
       application at
       <a href="#">©uminchu.com</a>.
    </h2>
    <%= link_to "アカウント作成", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
