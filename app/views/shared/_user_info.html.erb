<%= link_to gravatar_for(current_user, size: 50 ), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "自分のツイート", current_user %></span>
<span>ツイート数 (<%= current_user.microposts.count %>)</span>
